# Git: основи, правила та корисні практики

## 🎯 Мета уроку

* Зрозуміти, **навіщо потрібен Git** і як він працює
* Навчитися працювати з репозиторіями локально та віддалено
* Освоїти **branching**, **merge**, **pull request**
* Засвоїти базові **правила командної роботи**

---

## ⚙️ Початкове налаштування Git

Перед першою роботою з Git **обовʼязково** потрібно вказати імʼя та email.
Ці дані записуються в кожен commit і показують, **хто автор змін**.

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

Перевірка налаштувань:

```bash
git config --global --list
```

📌 Пояснення:

* `user.name` — ваше імʼя або нікнейм
* `user.email` — email (зазвичай той самий, що на GitHub / GitLab)
* `--global` — застосовується для всіх репозиторіїв

❗ Якщо email не співпадає з GitHub — коміти не будуть привʼязані до акаунта.

---

## 📌 Що таке Git

**Git** — це розподілена система контролю версій, яка дозволяє:

* зберігати історію змін коду
* працювати паралельно кільком розробникам
* безпечно експериментувати з кодом
* швидко повертатися до попередніх версій

Git ≠ GitHub

* **Git** — інструмент
* **GitHub / GitLab / Bitbucket** — платформи для хостингу репозиторіїв

---

## 🧠 Базові поняття

* **Repository (repo)** — проєкт з історією змін
* **Commit** — зафіксований стан файлів
* **Branch** — окрема гілка розробки
* **HEAD** — поточний commit
* **Remote** — віддалений репозиторій

---

## ⚙️ Базові команди Git

```bash
git init                # ініціалізація репозиторію
git status              # поточний стан
git add .               # додати файли до stage
git commit -m "message" # створити commit
git log                 # історія комітів
```

Робота з remote:

```bash
git remote add origin URL
git push origin main
git pull origin main
git clone URL
```

---

## 🌳 Branching (гілки)

### Навіщо потрібні гілки

* ізоляція фіч
* безпечні експерименти
* паралельна робота

### Основні команди

```bash
git branch               # список гілок
git branch feature-x     # створити гілку
git checkout feature-x  # перейти в гілку
git checkout -b feature-x # створити і перейти
git merge feature-x     # злити гілку
```

---

## 📐 Правила бранчингу (рекомендована схема)

### Основні гілки

* **main / master** — стабільна версія
* **develop** — активна розробка (опціонально)

### Робочі гілки

* `feature/login-form`
* `bugfix/fix-null-pointer`
* `hotfix/crash-on-start`

---

## 🐞 Bugfix vs 🚑 Hotfix

### Bugfix

**Bugfix** — виправлення помилки в коді, яка:

* знайдена під час розробки або тестування
* **не ламає продакшн**
* може почекати наступного релізу

📌 Характеристики:

* робиться від `develop` або `feature`
* потрапляє в реліз разом з іншими змінами
* без паніки 😄

Приклад:

```
bugfix/fix-login-validation
```

---

### Hotfix

**Hotfix** — термінове виправлення критичної помилки, яка:

* **ламає продакшн**
* блокує користувачів
* потребує негайного релізу

📌 Характеристики:

* робиться **від main**
* мінімальні зміни
* одразу деплоїться

Приклад:

```
hotfix/fix-crash-on-start
```

---

### Ключова різниця

| Bugfix                   | Hotfix            |
| ------------------------ | ----------------- |
| Не критичний             | Критичний         |
| Може чекати              | Терміновий        |
| Входить у плановий реліз | Деплоїться одразу |
| З develop                | З main            |

---

## 🔀 Merge vs Rebase

### Merge

* зберігає повну історію
* безпечний для командної роботи
* створює merge commit

### Rebase

* переписує історію
* робить її лінійною
* ❗ не використовувати для shared-гілок

---

## ⚔️ Merge Conflicts (конфлікти злиття)

### Що таке merge conflict

**Merge conflict** виникає тоді, коли Git не може автоматично об'єднати зміни, тому що:

* один і той самий рядок коду змінений у різних гілках
* файл був видалений в одній гілці і змінений в іншій

Git зупиняє merge і просить розробника вирішити конфлікт вручну.

---

### Як виглядає конфлікт у файлі

```text
<<<<<<< HEAD
old version
=======
new version
>>>>>>> feature-branch
```

---

### Як вирішувати merge conflict

1. Відкрити файл з конфліктом
2. Вибрати правильний варіант коду (або об'єднати обидва)
3. Видалити службові маркери `<<<<<<<`, `=======`, `>>>>>>>`
4. Додати файл у stage:

```bash
git add conflicted_file.py
```

5. Завершити merge:

```bash
git commit
```

---

### Корисні поради

* роби `git pull` **часто**
* малі PR → менше конфліктів
* не працюй довго в одній гілці
* використовуй IDE (IntelliJ, VS Code) для вирішення конфліктів

---

## 🔎 Pull Request / Merge Request

Що це:

* запит на злиття гілки
* code review
* автоматичні тести

Правила хорошого PR:

* маленький за обсягом
* зрозумілий опис
* одна логічна задача

---

## 🧾 Commit messages (дуже важливо!)

### Погано

```
fix
update
changes
```

### Добре

```
Add user authentication
Fix validation for email field
Refactor payment service
```

Формула:

> **Дієслово + що зроблено** (англійською, present tense)

---

## 🚫 Типові помилки

* коміти "раз на тиждень"
* робота без гілок
* великі PR
* force push у main
* ігнорування conflict'ів

---

## 📎 Корисні посилання

* Офіційна документація: [https://git-scm.com/doc](https://git-scm.com/doc)
* Інтерактивний туторіал: [https://learngitbranching.js.org](https://learngitbranching.js.org)
* Git cheatsheet: [https://education.github.com/git-cheat-sheet-education.pdf](https://education.github.com/git-cheat-sheet-education.pdf)
* GitHub Guides: [https://guides.github.com](https://guides.github.com)

---

## 🏁 Підсумок

Git — це **must-have** інструмент для будь-якого розробника.
Вміння правильно працювати з гілками, комітами та PR — ключ до командної розробки.

---

## 🧠 Домашнє завдання

1. Створити Git-репозиторій
2. Створити гілку `feature/example`
3. Зробити 3–5 осмислених commit'ів
4. Злити гілку в main через Pull Request
5. Описати, що було зроблено і які команди використовувались